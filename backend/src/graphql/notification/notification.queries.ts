const notifications = async (args, res) => {
  try {
    return null;
  } catch (error) {
    return new Error(error);
  }
};

const notificationById = async (args, res) => {
  try {
    return null;
  } catch (error) {
    return new Error(error);
  }
};

const notificationQueries = {
  notifications,
  notificationById
};

export default notificationQueries;
